---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- You can @ files here
- You can use markdown but dont have to
1、新增页面在src/router/index.ts 增加路由
2、swagger生成的接口在：swaggerapi.json，在请求后端根据swaggerapi.json接口请求
3、每个页面保证ui组件库：'naive-ui' 要按需引入，驼峰命名式使用，比如 
import { NButton, NCheckbox, NInput, useMessage } from 'naive-ui'        
 <NButton style="width: 200px;" type="primary" @click="handleLogin()">
          登录
        </NButton>

4、面实现使用setup ts 模式
5、后端接口url默认请求 src/api/server.ts的 baseSeverUrl